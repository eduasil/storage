name: 🚀 Deploy Storage Account

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      destroy:
        description: 'Destruir infraestrutura?'
        required: true
        default: 'false'

permissions:
  id-token: write
  contents: read

jobs:
  validate:
    name: 🔎 Validar Acessos e Terraform 
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.destroy != 'true' }}

    env:
      TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN_app_terraform_io }}
      ARM_CLIENT_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).clientId }}
      ARM_CLIENT_SECRET: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).clientSecret }}
      ARM_SUBSCRIPTION_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).subscriptionId }}
      ARM_TENANT_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).tenantId }}

    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v4

      - name: 🔐 Login no Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZ_GITHUB_ACTION_SP }}

      - name: 🛠️ Instalar Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.13.4

      - name: ✅ Validar Sintaxe e Plano
        run: |
          terraform init -input=false
          terraform fmt -check
          terraform validate
          terraform plan -input=false

  deploy:
    name: 🚀 Aplicar Storage Account Azure
    needs: validate
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.destroy != 'true' }}

    env:
      TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN_app_terraform_io }}
      ARM_CLIENT_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).clientId }}
      ARM_CLIENT_SECRET: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).clientSecret }}
      ARM_SUBSCRIPTION_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).subscriptionId }}
      ARM_TENANT_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).tenantId }}

    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v4

      - name: 🔐 Login no Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZ_GITHUB_ACTION_SP }}

      - name: 🛠️ Instalar Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.13.4

      - name: 🚀 Executar Apply
        run: |
          terraform init -input=false
          terraform apply -auto-approve

  destroy:
    name: 🧨 Destruir Infraestrutura
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.destroy == 'true' }}

    env:
      TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN_app_terraform_io }}
      ARM_CLIENT_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).clientId }}
      ARM_CLIENT_SECRET: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).clientSecret }}
      ARM_SUBSCRIPTION_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).subscriptionId }}
      ARM_TENANT_ID: ${{ fromJson(secrets.AZ_GITHUB_ACTION_SP).tenantId }}

    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v4

      - name: 🔐 Login no Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZ_GITHUB_ACTION_SP }}

      - name: 🛠️ Instalar Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.13.4

      - name: 🧨 Executar Destroy
        run: |
          terraform init -input=false
          terraform destroy -auto-approve
